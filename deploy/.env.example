SERVICE_NAME=rankr
SERVICE_DOMAIN=rankr.local
COMPOSE_PROJECT_NAME=rankr

GO_IMAGE_NAME=golang
GO_IMAGE_VERSION=1.25-alpine

# Shared infrastructure credentials (replace locally)
POSTGRES_USER=rankr_admin
POSTGRES_PASSWORD=change_me_admin
POSTGRES_DB=rankr

LEADERBOARDSTAT_USER=leaderboardstat_user
LEADERBOARDSTAT_PASS=change_me_stat

CONTRIBUTOR_USER=contributor_user
CONTRIBUTOR_PASS=change_me_contributor

PROJECT_USER=project_user
PROJECT_PASS=change_me_project

WEBHOOK_USER=webhook_user
WEBHOOK_PASS=change_me_webhook

LEADERBOARDSCORING_USER=leaderboardscoring_user
LEADERBOARDSCORING_PASS=change_me_leaderboardscoring

TASK_USER=task_user
TASK_PASS=change_me_task

AUTH_USER=auth_user
AUTH_PASS=change_me_auth

NOTIFAPP_USER=notification_user
NOTIFAPP_PASS=change_me_notification

REALTIME_USER=realtime_user
REALTIME_PASS=change_me_realtime

REDIS_PASSWORD=change_me_redis

NATS_USER=rankr
NATS_PASSWORD=change_me_nats

# ============================================
# BENCHMARK ENVIRONMENT (development-only)
# WARNING: These defaults are for local testing only.
# For CI/production benchmarks, override with secure values.
# ============================================
BENCHMARK_DB_NAME=webhook_db
BENCHMARK_DB_USER=webhook_admin
BENCHMARK_DB_PASS=benchmark_secret
BENCHMARK_INSERT_COUNT=1000
BENCHMARK_INSERT_RATE=1000
BENCHMARK_GRAFANA_PASS=admin123
BENCHMARK_NATS_AUTH=supersecret
