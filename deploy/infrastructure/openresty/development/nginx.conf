events {}

http {
    # Docker DNS resolver for dynamic upstreams.
    resolver 127.0.0.11 valid=10s ipv6=off;
    # Include all service-specific configs
#    include /etc/openresty/conf.d/common.conf;
    include /usr/local/openresty/nginx/conf/conf.d/*.conf;
#    include /etc/openresty/conf.d/leaderboard.conf;
#    include /etc/openresty/conf.d/contributor.conf;
}
